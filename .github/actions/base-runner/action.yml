name: base runner

description: install the fixed ubuntu ci toolchain

runs:
  using: composite
  steps:
    - name: install ci dependencies
      shell: bash
      run: |
        set -euo pipefail
        bash ./scripts/install-dev-deps
